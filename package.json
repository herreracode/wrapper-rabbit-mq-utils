{
  "name": "wrapper-rabbit-mq-utils",
  "version": "0.0.1",
  "description": "wrapper rabbit mq utils for consume and publish events with secure",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "test": "jest",
    "clear-cache": "jest --clearCache"
  }
}
